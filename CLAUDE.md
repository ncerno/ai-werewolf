# AI 狼人杀 · 12 人局模拟器

## 项目目标
构建完全自动化的狼人杀游戏。12 名玩家全部由独立 LLM Agent 扮演，人类用户通过 Web 前端旁观或参与游戏。

三种运行模式：
1. **旁观模式**：全 AI 自动对决，用户看实时日志
2. **上帝模式**：用户当上帝，操控游戏进程，AI 做决策
3. **玩家模式**：用户以玩家身份参与，AI 当上帝，其余 AI 为玩家

## 技术栈
- **语言**：Python 3.10+
- **LLM 调用**：openai 库（兼容 OpenAI 格式 API，支持 DeepSeek、Qwen 等）
- **后端**：FastAPI + WebSocket（实时推送）
- **前端**：原生 HTML/JS（单页应用）
- **并发**：asyncio
- **配置**：独立 config 文件 + 前端配置页面
- **部署**：可部署到域名，任意设备访问

## 目录规范

```
ai-werewolf/
├── docs/               # 方案书、接口调研、开发计划
├── src/                # 源码根目录
│   ├── agent/          # PlayerAgent：LLM Agent 封装、prompt 构建、行动解析
│   ├── engine/         # 游戏引擎：状态管理、回合调度、规则判定
│   ├── server/         # FastAPI 后端：REST API、WebSocket、路由
│   ├── frontend/       # 前端静态文件：HTML、CSS、JS
│   └── utils/          # 工具：日志、配置加载、格式化
├── config/             # 默认配置文件
├── tests/              # 测试
└── data/               # 运行时数据（游戏日志、用户配置等）
```

新增目录或文件前，先判断是否能放入现有结构；能复用就不新增。
临时文件只允许放在 `src/utils/` 管理。

## 游戏规则（硬编码引擎）

### 角色配置
- 12 人局：4 狼人 + 4 神（预言家/女巫/猎人/白痴）+ 4 平民
- 警长：附加身份，第一天白天竞选产生，1.5 票，最后发言/归票

### 胜利条件（屠边）
- 狼人获胜：所有神出局 OR 所有平民出局，或狼数 >= 存活好人数
- 好人获胜：所有狼人出局

### 夜晚阶段
唤醒顺序：狼人 → 女巫 → 预言家 → 猎人 → 白痴（仅首夜）

- **狼人**：独立投票多数决。支持 SKIP 空刀。平票随机选平票目标
- **女巫**：解药+毒药各一瓶，同晚只能用一瓶。可自救。上帝只通报"谁死了/平安夜"
- **预言家**：查验一名玩家，返回狼人/好人
- **猎人**：每晚被告知开枪状态（可用/不可用）
- **白痴**：仅首夜睁眼确认身份

### 白天阶段
1. 天亮 → 宣布死讯（公布死者编号，不公布身份）
2. 第一天天亮：警长竞选（上警发言 → 退水 → 投票 → PK）
3. 遗言：第一夜死者有遗言
4. 发言：警长定警左/警右。无警长时上帝决定
5. 投票放逐 → 平票则 PK 再投，仍平票则无人出局
6. 被放逐者遗言

### 特殊判定
- **猎人**：被投票放逐时自主决定开枪。被毒杀/殉情不能开枪
- **白痴**：被放逐翻牌免死，丧失投票权但可发言。被刀/毒直接死
- **女巫**：解药用完后不再获知夜晚死者
- **狼刀在先**：狼人杀最后一个好人且女巫不用解药 → 直接判狼人胜，女巫后手毒无效

## 核心架构设计

### PlayerAgent 类 (`src/agent/player.py`)
```
属性：player_id, role, model_name, model_config, memory, system_prompt
方法：
  _build_system_prompt()     → 角色专属提示词（身份隔离）
  observe(event: str)        → 追加信息到 memory
  async decide(action_hint)  → 调用 LLM 返回决策文本
  async summarize()          → 每两回合自我总结，压缩 memory
```

### ActionParser (`src/agent/parser.py`)
从 LLM 返回文本中提取指令：
- 格式要求：`【行动】ACTION TARGET_ID`
- 支持：KILL, CHECK, VOTE, SHOOT, SKIP, SAVE, POISON, GIVE_BADGE
- 正则提取 → 合法性校验 → 无效时重试一次 → 仍失败则随机合法行动
- 规则引擎 100% 拦截非法行动（目标已死、无权限、重复用药等）

### Game 引擎 (`src/engine/`)
```
GameState：所有状态数据（玩家列表、存活状态、回合、阶段、狼刀目标等）
RuleEngine：胜利判定、合法性校验、投票计票
GameController：回合调度、阶段推进、并发控制
```

### LLM 输出可靠性设计
1. Prompt 硬约束输出格式：必须包含 `【行动】ACTION ID`
2. 代码正则提取 + 合法性校验
3. 格式不匹配 → 原消息+错误说明发回 LLM 重试一次
4. 重试仍失败 → 代码强制选择随机合法行动
5. LLM 只表达意图，规则引擎保证正确性

## 前端功能

### 页面结构
- 主页：模式选择（旁观/上帝/玩家）
- 设置页：模型配置、角色分配、参数调整、API Key
- 游戏页：
  - 主日志流（实时游戏过程、发言、行动）
  - 私密信息面板（模式三玩家：身份牌、狼队友、查验/用药结果、上帝反馈）
  - 输入区（模式二上帝指令、模式三玩家发言）
  - 状态栏（当前回合、阶段、存活人数）

### 实时推送
- WebSocket 连接，游戏事件实时推送到前端
- 日志分级：公开日志 / 私密日志（仅特定玩家可见）

## 配置管理
- 默认配置：`config/default.yaml`
- 用户配置：通过前端设置页修改，持久化到 `data/user_config.json`
- 可配置项：
  - LLM 模型列表（支持按角色分别指定 model + api_base + api_key）
  - Temperature（默认 0.7）
  - 单个/多模型模式切换
  - 发言时间上限、投票时间上限
  - 游戏日志输出路径

## 并发策略
- 夜晚阶段：狼人投票 → 串行询问各狼人 → 异步收集结果
- 发言阶段：按顺序发言，不可并行
- 投票阶段：所有存活玩家异步调用 LLM → 收集投票
- WebSocket：asyncio 原生异步推送

## 命名约定
- 文件名：snake_case
- 类名：PascalCase
- 函数/变量：snake_case
- 配置项：PascalCase 或保持与 AstrBot 一致

## 开发纪律
- 开发前先读本文件，再动手
- 每个阶段完成必须做验证
- 大改动先进 Plan Mode，确认后再实施
- 改规范先改 CLAUDE.md，再改实践
- 密钥、token 不进代码、不进提交、不进日志
- 不为了跑通而注释报错或绕过校验

## 验证命令
- `python -c "import openai; print('ok')"` — 检查 openai 依赖
- `python -c "from fastapi import FastAPI; print('ok')"` — 检查 fastapi 依赖
- `python -m pytest tests/ -v` — 运行测试
- `python src/main.py` — 启动服务（后续补充）
